{
  "thought": "I am synthesizing the definitive Next.js 15 and TypeScript standards, incorporating mandatory authentication/authorization for Server Actions, error masking to prevent information exposure, and updated Next.js 15 caching defaults. I will use the write_file tool to ensure the content is saved correctly without JSON parsing errors.",
  "tool": "write_file",
  "payload": "{\"path\": \"knowledge/nextjs_ts_standards.md\", \"content\": \"# Next.js 15, RSC & Strict TypeScript Enterprise Standards (v1.1)\\n\\n## 1. Core Architectural Principles\\n- **Server-First by Default:** All components are React Server Components (RSC) unless interactivity (hooks, event listeners) is strictly required. Push client boundaries to the leaves of the component tree.\\n- **Zero-Trust Server Actions:** Treat every Server Action as a public POST endpoint. Always perform independent Authentication and Authorization (AuthZ) inside the action body.\\n- **Type-Safe Boundaries:** Use Zod for runtime validation at all network (API/Server Actions) and database boundaries. Enforce strict TypeScript (`strict: true`) to eliminate `any` and `implicitAny`.\\n- **Performance-Obsessed Caching:** Understand that Next.js 15 defaults to `no-store`. Explicitly opt-in to caching for static or semi-static data to minimize TTFB.\\n\\n## 2. Strict TypeScript Standards\\n- **No `any` Policy:** Use `unknown` with type guards or Zod for external data. Use discriminated unions for complex state.\\n- **Branded Types:** Use branded types for IDs (e.g., `UserId`, `OrderId`) to prevent logic errors in service layers.\\n- **Strict Function Returns:** Always define return types for exported functions to ensure contract stability.\\n\\n```typescript\\n// Example: Strict Type Narrowing with Zod\\nimport { z } from 'zod';\\n\\nconst UserSchema = z.object({\\n  id: z.string().uuid(),\\n  email: z.string().email(),\\n});\\n\\ntype User = z.infer<typeof UserSchema>;\\n```\\n\\n## 3. Data Fetching & Caching (Next.js 15 Update)\\n- **Uncached by Default:** Next.js 15 `fetch` requests default to `no-store`.\\n- **Opt-in Caching:** Use `cache: 'force-cache'` for data that changes infrequently.\\n- **Revalidation:** Use `next: { revalidate: 3600 }` for Incremental Static Regeneration (ISR).\\n\\n```typescript\\n// ✅ Good: Explicit caching for static content\\nconst data = await fetch('https://api.example.com/data', { \\n  cache: 'force-cache' \\n});\\n```\\n\\n## 4. Server Actions: Security & Reliability\\n### A. Authentication & Authorization (AuthZ)\\nServer Actions are public endpoints. You **MUST** verify the user's identity and permissions inside every action.\\n\\n```typescript\\nexport async function updateSensitiveData(id: string, formData: FormData) {\\n  const session = await getAuthSession();\\n  if (!session) throw new Error('Unauthorized');\\n  \\n  const userPermissions = await getPermissions(session.user.id);\\n  if (!userPermissions.canEdit) throw new Error('Forbidden');\\n\\n  // Proceed with validated logic...\\n}\\n```\\n\\n### B. Error Masking (Information Exposure)\\nNever return raw database or system errors to the client. Log the full error on the server and return a sanitized message.\\n\\n```typescript\\n// ✅ Good: Masking internal errors\\ncatch (err) {\\n  console.error('[DATABASE_ERROR]:', err); // Internal log\\n  return { success: false, error: 'An internal error occurred. Please try again.' };\\n}\\n```\\n\\n### C. Validation\\nAlways validate `formData` or arguments using Zod before processing.\\n\\n## 5. Component Patterns\\n- **Composition over Configuration:** Use the `children` prop to nest Client Components inside Server Components to maintain server-side rendering for the majority of the tree.\\n- **Error Boundaries:** Use `error.tsx` at the route level to handle runtime exceptions gracefully.\\n- **Loading UI:** Use `loading.tsx` for instant loading states via React Suspense.\"}"
}