I will now synthesize the final Next.js and TypeScript standards, incorporating the "Gold Standard" templates to resolve all previous security, logic, and type-safety issues, and save the document to the requested path.

```json
{
  "thought": "I am generating the final, high-authority Next.js and TypeScript standards document. This version incorporates Next.js 15 async boundaries, strict Zod-based data validation at the network edge, SSRF-safe fetching patterns, and prototype-pollution-resistant Server Action handling using zod-form-data. It explicitly bans 'any' and type assertions in favor of runtime validation.",
  "tool": "write_file",
  "payload": {
    "path": "knowledge/nextjs_ts_standards.md",
    "content": "# Next.js & Strict TypeScript Engineering Standards (v1.1)\n\n## 1. Core Identity & Type Safety\n- **Strict Mode:** All projects must enable `\"strict\": true` in `tsconfig.json`.\n- **Zero-Tolerance for `any`:** The use of `any` or `@ts-ignore` is strictly forbidden. Use `unknown` for values of uncertain shape and narrow them using Type Guards or Zod schemas.\n- **No Type Assertions:** Never use `as T` for external data (API responses, form inputs). Always **parse** and **validate** at the boundary.\n\n## 2. Next.js 15 Async Boundaries\nAs of Next.js 15, `params` and `searchParams` are asynchronous. They must be typed as `Promise` and awaited before access in Pages, Layouts, and Route Handlers.\n\n```typescript\ntype PageProps = {\n  params: Promise<{ id: string }>;\n  searchParams: Promise<{ [key: string]: string | string[] | undefined }>;\n};\n\nexport default async function Page({ params, searchParams }: PageProps) {\n  const resolvedParams = await params;\n  const resolvedSearch = await searchParams;\n\n  const userId = resolvedParams.id;\n  const query = typeof resolvedSearch.q === 'string' ? resolvedSearch.q : '';\n\n  return <main>User ID: {userId}</main>;\n}\n```\n\n## 3. Robust & Secure Server Actions\nServer Actions are public POST endpoints. They must implement strict access control, validation, and error handling.\n\n### The Standard Pattern:\n1. **Generic Response Type:** Use a standardized response object (no `any`).\n2. **`zod-form-data`:** Mandatory for parsing `FormData`. This prevents prototype pollution and correctly handles multi-value inputs.\n3. **Access Control:** Always verify the session *inside* the action.\n\n```typescript\n'use server';\n\nimport { z } from 'zod';\nimport { zfd } from 'zod-form-data';\nimport { auth } from '@/lib/auth';\n\nexport type ActionResponse<T = void> = \n  | { success: true; data: T } \n  | { success: false; error: string; fieldErrors?: Record<string, string[]> };\n\nconst UpdateSchema = zfd.formData({\n  id: zfd.text(z.string().uuid()),\n  roles: zfd.repeatable(z.array(z.string())),\n  isActive: zfd.checkbox(),\n});\n\nexport async function updateAction(formData: FormData): Promise<ActionResponse> {\n  try {\n    const session = await auth();\n    if (!session?.user) return { success: false, error: 'Unauthorized' };\n\n    const parsed = UpdateSchema.safeParse(formData);\n    if (!parsed.success) {\n      return {\n        success: false,\n        error: 'Validation failed',\n        fieldErrors: parsed.error.flatten().fieldErrors,\n      };\n    }\n\n    // Execute mutation...\n    return { success: true, data: undefined };\n  } catch (e) {\n    return { success: false, error: 'Internal Server Error' };\n  }\n}\n```\n\n## 4. SSRF-Safe & Type-Safe Data Fetching\n### SSRF and Path Traversal Protection\nNever interpolate raw variables into URLs. Use the `URL` object and `encodeURIComponent` to ensure proper sanitization.\n\n### Validation Boundary\nAlways parse JSON responses through a Zod schema. Do not use `as Promise<T>`.\n\n```typescript\nimport { z } from 'zod';\n\nconst UserSchema = z.object({ id: z.string(), name: z.string() });\ntype User = z.infer<typeof UserSchema>;\n\nexport async function getUser(id: string): Promise<User | null> {\n  const baseUrl = process.env.API_BASE_URL;\n  if (!baseUrl) throw new Error('API_BASE_URL missing');\n\n  // Automatic encoding via URL object\n  const url = new URL(`/users/${encodeURIComponent(id)}`, baseUrl);\n\n  try {\n    const res = await fetch(url.toString());\n    if (!res.ok) return null;\n\n    const json = await res.json();\n    const parsed = UserSchema.safeParse(json);\n    \n    if (!parsed.success) throw new Error('API Contract Violation');\n    return parsed.data;\n  } catch (error) {\n    console.error('[getUser]', error);\n    throw error;\n  }\n}\n```\n\n## 5. Zod Transformation & Default Logic\nTo prevent logic bugs and type mismatches between Input and Output types:\n- **Order Matters:** `.default()` must precede `.transform()`.\n- **Default Value:** The `.default()` value must match the **Input** type of the schema.\n\n```typescript\n// ✅ Correct: Default matches string input, then transforms to boolean\nconst schema = z.string()\n  .default('false')\n  .transform((val) => val === 'true');\n\n// ❌ Incorrect: .default(false) after .transform() causes TypeScript error\n// ❌ Incorrect: .default('false') after .transform() causes logic bug (string 'false' is truthy)\n```\n\n## 6. Component Architecture (RSC First)\n- **Default to Server Components:** Ship zero JS by default. Use `\"use client\"` only for hooks (`useState`, `useEffect`) or event listeners.\n- **Colocation:** Keep styles, types, and sub-components in the same directory as the route segment.\n- **Suspense Boundaries:** Wrap data-heavy components in `<Suspense />` to enable streaming SSR."
  }
}
```