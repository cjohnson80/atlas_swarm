{"thought": "I will use a shell heredoc to write the comprehensive AI Agent Patterns documentation to avoid JSON escaping issues and ensure the content meets the high standards of the Principal Cognitive Architect persona.", "tool": "run_shell", "payload": "mkdir -p knowledge/ && cat << 'EOF' > knowledge/ai_agent_patterns.md\n# AI Agent Architectures & Cognitive Patterns\n\n## 1. Reasoning Frameworks\n\n### ReAct (Reason + Act)\n**Mechanism:** A paradigm that interleaves reasoning traces (thoughts) with environment-specific actions. The agent generates a 'Thought' explaining its logic, followed by an 'Action' (tool call), and then receives an 'Observation' from the environment.\n- **Prompt Structure:** `Question -> Thought -> Action -> Observation -> Thought...` \n- **Trade-offs:** High reliability and interpretability, but can suffer from 'looping' if the observation doesn't provide enough signal to change the thought process. Higher latency due to multiple LLM calls.\n\n### Plan-and-Solve\n**Mechanism:** Unlike ReAct's step-by-step nature, Plan-and-Solve requires the agent to first generate a comprehensive plan (sequence of sub-tasks) before executing any actions.\n- **Mechanism:** `Goal -> Plan (Sub-tasks 1..N) -> Execution Loop -> Final Answer`.\n- **Trade-offs:** Significantly reduces 'drift' in complex tasks. More token-efficient than ReAct for predictable workflows but less adaptable to unexpected tool outputs mid-execution.\n\n## 2. Memory Management Architectures\n\n### Working Memory (Short-term)\n- **Definition:** The active context window containing the current conversation and immediate task state.\n- **Optimization:** Context pruning, summarization of distant history, and 'Scratchpads' to keep the most relevant data in the high-attention area of the window.\n\n### Episodic Memory (Event-based)\n- **Definition:** A log of past experiences and tool-use outcomes.\n- **Architecture:** Often implemented as a vector store where past 'episodes' (successful or failed task completions) are retrieved via semantic similarity to inform current strategy.\n\n### Semantic Memory (Knowledge-based)\n- **Definition:** Persistent, structured knowledge (e.g., documentation, codebase facts).\n- **Retrieval:** RAG (Retrieval-Augmented Generation) using hierarchical indexing or knowledge graphs to provide the agent with 'ground truth' facts without fine-tuning.\n\n## 3. Multi-Agent Orchestration Topologies\n\n| Topology | Mechanism | Use Case |\n| :--- | :--- | :--- |\n| **Hierarchical** | A 'Manager' agent decomposes tasks and assigns them to specialized 'Workers'. | Complex software engineering, project management. |\n| **Sequential** | Agents operate in a pipeline (e.g., Coder -> Reviewer -> DevOps). | Standardized CI/CD or content generation. |\n| **Joint-Consensus** | Multiple agents vote on an output to reduce hallucinations. | High-stakes decision making or code auditing. |\n\n## 4. Tool-Use Optimization\n\n- **Strict Schema Adherence:** Utilizing JSON Schema or Pydantic models to force the LLM into valid function-call structures.\n- **Few-Shot Prompting:** Providing examples of correct tool calls and, crucially, examples of how to handle tool errors.\n- **Error-Handling Loops:** Implementing a 'Self-Correction' step where the agent is fed its own tool-call error message and asked to fix the arguments.\n- **Latency Mitigation:** Parallel tool execution (calling multiple independent tools in one turn) to reduce total round-trip time.\n\n--- \n*Documented by AtlasSwarm Principal Cognitive Architect*\nEOF\n"}